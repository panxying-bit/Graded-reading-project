/** Product version (keep in sync with root / web / server package.json). */
export const APP_VERSION_SEMVER = "5.4.0";

/** Short label shown in UI (e.g. v5.4). */
export const APP_VERSION_SHORT = "5.4";

/** Release marker for this version line (shown next to version in the shell). */
export const APP_VERSION_TAG = "单课生成稳定";
