/** Product version (keep in sync with root / web / server package.json). */
export const APP_VERSION_SEMVER = "5.1.0";

/** Short label shown in UI (e.g. v5.1). */
export const APP_VERSION_SHORT = "5.1";

/** Release marker for this version line (shown next to version in the shell). */
export const APP_VERSION_TAG = "Volc 诊断与存档点";
