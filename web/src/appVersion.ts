/** Product version (keep in sync with root / web / server package.json). */
export const APP_VERSION_SEMVER = "5.2.0";

/** Short label shown in UI (e.g. v5.2). */
export const APP_VERSION_SHORT = "5.2";

/** Release marker for this version line (shown next to version in the shell). */
export const APP_VERSION_TAG = "课文·图片·知识点 三模块";
