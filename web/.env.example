# Optional: when the backend runs on a port other than 3000, set this so Vite
# dev proxy matches (e.g. VITE_DEV_API_PORT=3010).
# VITE_DEV_API_PORT=3001
#
# Dev proxy /api timeout is 15m in vite.config.ts so Volc Jimeng image poll can finish.
