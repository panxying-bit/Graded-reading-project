{
  "name": "graded-reading-platform",
  "version": "5.2.0",
  "private": true,
  "description": "5.2 — 课文 / 图片 / 知识点 三模块工作台（Tab 单挂载，减轻渲染）",
  "scripts": {
    "llm:ping": "node server/scripts/llm-ping.mjs",
    "dev": "npm run dev:all",
    "dev:stop": "bash scripts/stop-dev-stack.sh",
    "dev:fresh": "npm run dev:stop && npm run dev:all",
    "dev:all": "concurrently -k -n server,web -c blue,magenta \"npm run server:dev\" \"npm run web:dev\"",
    "dev:server": "npm run server:dev",
    "web:dev": "npm --prefix web run dev",
    "server:dev": "npm --prefix server run dev",
    "server:dev:once": "npm --prefix server run dev:once",
    "server:build": "npm --prefix server run build",
    "server:wordlist": "npm --prefix server run build:wordlist"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
